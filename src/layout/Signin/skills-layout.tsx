import { Container } from "@mui/material"

export const SkillsLayout = () => {
  return (
    <Container maxWidth={false} sx = {{
      background: '#f3f2f0',
      width: '100%',
      height: '40vh'
    }}
    >
    </Container>
  )
}