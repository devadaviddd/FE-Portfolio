import { Container } from "@mui/material"

export const CVLayout = () => {
  return (
    <Container maxWidth={false} sx={{
      width: "100%",
      height: "40vh",
      background: "#dde9f5"
    }}>
    </Container>
  )
}